{
  "name": "ProtractorWithPageObjects",
  "version": "1.0.0",
  "description": "A simple protractor project that has page objects",
  "author": "Hayden Meloche",
  "dependencies": {
    "protractor": "^5.1.2",
    "webdriver-manager": "^12.0.6"
  },
  "scripts": {
    "prestart-webdriver": "webdriver-manager update",
    "webdriver": "webdriver-manager start",

    "searchTest": "protractor conf.js --suite searchTest"
  }
}
